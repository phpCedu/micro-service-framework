{
    // ALL methods can also return an exception, perhaps ... hmm, how will that play with futures?
    'childReverse': {
        'type': 'string',
        // problem is, structs aren't ordered, so this is the most straightforward way, less nesting
        'argNames': ['name'],
        'argTypes': ['string']
    },
    'reverse': {
        'type': 'string',
        // structs aren't ordered, so need to use arrays
        // but how do optional params?
        'argNames': ['name'],
        'argTypes': ['string']
    }
}
